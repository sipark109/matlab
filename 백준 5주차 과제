#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int a, b;
	while (scanf("%d %d", &a, &b) != EOF) {
		printf("%d\n", a + b);
	}

	return 0;
}
#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int a, b;
	while (1) {
		scanf("%d %d", &a, &b);
		if (a == 0 && b == 0)break;
		printf("%d\n", a + b);
	}

	return 0;
}

#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int n;scanf("%d", &n);
	for (int i = 1;i <= n;i++) {
		for (int j = n;j > i;j--) {
			printf(" ");
		}
		for (int k = 1;k <= i;k++) {
			printf("*");
		}
		printf("\n");
	}
	return 0;
}
#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int n;scanf("%d", &n);
	for (int i = 1;i <= n;i++) {
		for (int j = 1;j <= i;j++) {
			printf("*");
		}
		printf("\n");
	}
	return 0;
}

#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int t, a, b;scanf("%d", &t);
	for (int i = 0;i < t;i++) {
		scanf("%d %d", &a, &b);
		printf("Case #%d: %d + %d = %d \n", i + 1,a,b, a + b);
	}


	return 0;
}
#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int t, a, b;scanf("%d", &t);
	for (int i = 0;i < t;i++) {
		scanf("%d %d", &a, &b);
		printf("Case #%d: %d\n", i + 1, a + b);
	}


	return 0;
}
