#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int t,a,b;scanf("%d", &t);
	for (int i = 0;i < t;i++) {
		scanf("%d %d", &a, &b);
		printf("%d\n", a + b);
	}
	return 0;
}
#include<stdio.h>
#pragma warning(disable:4996)
int main() {
	int N,i;
	scanf("%d", &N);
	for (i = 1;i <= 9;i++) {
		printf("%d * %d = %d\n", N, i, N * i);
	}
	return 0;
}
#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int t;scanf("%d", &t);
	int a, b;
	for (int i = 0;i < t;i++) {
		scanf("%d %d", &a, &b);
		printf("%d\n", a + b);
	}
	return 0;}
#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int n;scanf("%d", &n);
	printf("%d\n", (n * (n + 1)) / 2);
	return 0;
}
#include<stdio.h>
#include<stdlib.h>
#pragma warning(disable:4996)
int main() {
	int total,n,a,b;scanf("%d", &total);
	scanf("%d", &n);
	for (int i = 0;i < n;i++) {
		scanf("%d %d", &a, &b);
		total=total - a * b;
	}
	if (total == 0) {
		printf("Yes");
	}
	else
		printf("No");
	return 0;
}
#include<stdio.h>
#pragma warning(disable:4996)
int main() {
	int n,i=1,x=0;
	scanf("%d", &n);
	for (i = 1;n/4>=i;i++) {
		printf("long ");}
	printf("int");
	return 0;}
